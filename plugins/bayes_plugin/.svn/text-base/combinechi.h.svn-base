#ifndef __COMBINECHI_H__
#define __COMBINECHI_H__
#include "linked_list.h"
#include <math.h>

#define SIGNIFFICANT_TOKENS 150

struct probability{
        char *token;
        double prob;
    };

typedef struct probability probability;

long double combine_by(linklist *list_prob,int ns, int nh);

#endif
